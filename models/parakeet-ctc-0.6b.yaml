# Model catalog entry for parakeet-ctc-0.6b
# This file defines the model variant metadata for runtime model management (M36)

schema_version: "1.1"
id: parakeet-ctc-0.6b
runtime: nemo
runtime_model_id: "nvidia/parakeet-ctc-0.6b"  # HuggingFace ID passed to from_pretrained()

name: NVIDIA Parakeet CTC 0.6B
source: nvidia/parakeet-ctc-0.6b
size_gb: 1.8
stage: transcribe

description: |
  NVIDIA Parakeet FastConformer CTC 0.6B for fast English-only transcription.
  CTC decoder provides fastest inference with greedy decoding.
  Produces native word-level timestamps without separate alignment.
  Best choice for high-throughput batch processing.

  NOTE: Outputs lowercase text without punctuation. Use refine stage or
  parakeet-tdt-0.6b-v3 if punctuation/capitalization is required.

languages:
  - en

capabilities:
  word_timestamps: true
  punctuation: false
  capitalization: false
  streaming: false
  max_audio_duration: 7200

hardware:
  min_vram_gb: 4
  supports_cpu: false
  min_ram_gb: 8

performance:
  rtf_gpu: 0.0003
  rtf_cpu: null
