schema_version: "1.1"
id: nvidia/diar-sortformer-4spk-v2.1
engine_id: nemo-sortformer
loaded_model_id: nvidia/diar-sortformer-4spk-v2.1

name: NVIDIA NeMo Sortformer 4-Speaker v2.1
source: nvidia/diar_streaming_sortformer_4spk-v2.1
size_gb: 0.9
stage: diarize

description: |
  End-to-end Transformer encoder diarization model from NVIDIA NeMo.
  Resolves the permutation problem via arrival-time ordering.
  Native overlap handling with low DER on ≤4 speaker scenarios.
  Improved over v2 on meeting-style audio (AMI SDM DER: 31% → 21%).

languages: null

capabilities:
  word_timestamps: false
  punctuation: false
  capitalization: false
  native_streaming: false
  max_audio_duration: 7200
  max_speakers: 4

hardware:
  min_vram_gb: 4
  supports_cpu: true
  min_ram_gb: 8

performance:
  rtf_gpu: 0.04
  rtf_cpu: 2.5
