# NeMo MSDD Diarization Engine Dependencies

# NeMo toolkit for ASR/diarization models
nemo_toolkit[asr]>=2.7.3,<2.8.0

# Audio processing
soundfile>=0.14.0

# Diarization-specific (explicit for clarity, pulled by nemo)
omegaconf>=2.3.1

# googleapis-common-protos >=1.75.1 (2026-08-06) requires protobuf>=6.33.5,
# but nemo_toolkit 2.7.x pins protobuf~=5.29.5. The OTLP gRPC exporter pulls
# googleapis-common-protos in via the dalston[engine-sdk] layer, and this
# file is the last pip layer, so the pin has to live here to keep the final
# image consistent (otherwise `import dalston.telemetry` crashes with a
# protobuf gencode/runtime VersionError). Drop when NeMo 3.x lands — it has
# no protobuf pin.
googleapis-common-protos<1.75.1
