# Parakeet ONNX Engine Dependencies
# Lightweight: no NeMo, no PyTorch required for CPU inference

# ONNX ASR library - handles model loading, preprocessing, and CTC/TDT decoding
# for NVIDIA NeMo Parakeet models via ONNX Runtime
onnx-asr[cpu,hub]>=0.3.3

# Audio processing
soundfile>=0.12.0
numpy>=1.24.0,<3.0.0
