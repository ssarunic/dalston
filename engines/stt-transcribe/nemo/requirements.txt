# Unified NeMo Engine Dependencies (batch + realtime)

# NeMo 3.0.x — the speech-only toolkit. Must match docker/Dockerfile.base-nemo;
# this line only re-asserts the range in the final pip layer. 3.0 leaves
# transformers and protobuf unpinned (2.7.x pinned transformers~=4.57 and
# protobuf~=5.29, which is what blocked GHSA-xrqw-3rrv-vx5w and the
# googleapis-common-protos conflict).
nemo_toolkit[asr]>=3.0.0,<3.1.0

# Audio processing
numpy>=2.4.6,<3.0.0
librosa>=0.11.0
soundfile>=0.14.0

# Required by Silero VAD (realtime adapter)
torchaudio

# Batch VAD chunking (M86) — bundles the Silero model so the base-engine
# VadChunker runs fully offline once installed. Used by long-audio
# requests that exceed DALSTON_NEMO_MAX_CHUNK_S. Small pure-Python
# package (~8 MB) with the JIT and ONNX weights embedded.
silero-vad==6.2.1
