# Phoneme Forced Alignment Engine Dependencies

# PyTorch for model inference
torch>=2.12.1
torchaudio>=2.11.0

# Audio loading backend (soundfile is cross-platform: ARM64, x86_64, with/without CUDA)
soundfile>=0.13.1

# HuggingFace transformers for wav2vec2 model loading (5.x required for huggingface_hub 1.x)
transformers>=5.9.0

# Audio and numerical processing
numpy>=2.4.4
