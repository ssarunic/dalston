# Unified NeMo Engine Dependencies (batch + realtime)

# Pin NeMo to a stable 2.7.x line for cache-aware RNNT streaming.
nemo_toolkit[asr]>=2.7.0,<2.8.0

# Align key transitive deps with NeMo 2.7 ASR metadata.
lhotse>=1.32.2
transformers>=4.57.0,<4.58.0

# Audio processing
numpy>=1.22.0,<3.0.0
librosa>=0.10.1
soundfile>=0.12.0

# Required by Silero VAD (realtime adapter)
torchaudio
