# Whisper Streaming Real-time Engine Dependencies

# Core transcription library (CTranslate2-based Whisper)
faster-whisper>=1.0.0

# PyTorch for GPU support, model handling, and Silero VAD
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
numpy>=1.24.0

# Note: websockets and redis are installed via dalston[realtime-sdk]
