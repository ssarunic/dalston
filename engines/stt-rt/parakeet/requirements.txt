# Parakeet Streaming Real-time Engine Dependencies

# NeMo toolkit for ASR models
nemo_toolkit[asr]>=1.23.0

# Audio processing
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0
torchaudio  # Required by Silero VAD

# Note: websockets and redis are installed via dalston[realtime-sdk]
