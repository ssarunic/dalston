# Voxtral realtime on vLLM

# vLLM with multimodal audio support
vllm[audio]>=0.6.0

# Audio utilities used by engine_id + tests
soundfile>=0.12.0
librosa>=0.10.0
numpy>=1.24.0

# PyTorch (CUDA required by vLLM)
torch>=2.0.0
