# ONNX Streaming Real-time Engine Dependencies

# ONNX ASR library for CTC/TDT/RNNT inference
onnx-asr[cpu,hub]>=0.5.0

# Audio processing
numpy>=1.24.0,<3.0.0
soundfile>=0.12.0
torchaudio  # Required by Silero VAD

# Note: websockets and redis are installed via dalston[realtime-sdk]
